use strict;
use warnings;

# Configuration du script
our $CONFIG = {
	'verbose' => 0,    # Pas de verbose (0)
	'quiet'   => 0,    # Pas de messages de compilation sur la sortie standard (0)
	'force'   => 0,    # Pas de force de la mise à jour (0)
};

1;

